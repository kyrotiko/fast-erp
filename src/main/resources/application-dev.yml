spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3307/fasterp?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  devtools:
    restart:
      enabled: true

logging:
  level:
    com.yy.fasterp.mapper: DEBUG